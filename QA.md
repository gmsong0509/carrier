# Carrier GreenON 배포 전 QA

자동 검사는 `npm test`와 `npm run build`로 실행합니다. 아래 항목은 실제 이메일, 브라우저 세션 또는 배포 URL이 필요하므로 Render 배포 후 수동으로 확인합니다.

## 준비

- Supabase Auth의 Site URL과 Redirect URLs에 Render HTTPS 주소가 등록되어 있는지 확인
- 일반 사용자 테스트 계정 A와 B 준비
- 두 계정의 비밀번호나 인증 토큰은 문서·소스·채팅에 기록하지 않음
- 모바일 360px, 태블릿 768px, 데스크톱 화면 준비

## 사용자 흐름

1. 계정 A로 회원가입하고 이메일 확인 후 로그인한다.
2. 서울 현재 온도·습도와 가상 에어컨 정상 상태가 Blue 계열로 표시되는지 확인한다.
3. 오늘의 GREEN MISSION에 참여한다.
4. COOL, 26°C 이상, 정상 센서 상태에서 30분 진행을 네 번 실행한다.
5. 미션이 성공하고 GREEN POINT 300P가 정확히 한 번 적립되는지 확인한다.
6. 새로고침 후 미션·포인트·에어컨 상태가 유지되는지 확인한다.
7. REWARD SHOP에서 300P 이하 상품을 구매하고 잔액 차감·구매내역을 확인한다.
8. 포인트보다 비싼 상품 구매 시 결제가 거절되고 Red 경고가 표시되는지 확인한다.
9. 필터 점검, 센서 오류, 26°C 미만, 미션 중 전원 OFF에서 Red 상태를 확인한다.
10. GREEN REPORT의 완료 미션·절감량·구매 건수가 서버 데이터와 일치하는지 확인한다.

## 사용자 데이터 분리

1. 다른 브라우저 또는 시크릿 창에서 계정 B로 로그인한다.
2. 계정 A의 포인트, 미션, 구매내역이 보이지 않는지 확인한다.
3. 계정 B에서 만든 기록이 계정 A에 나타나지 않는지 확인한다.
4. 로그아웃 뒤 개인 데이터 API가 401 또는 빈 결과로 차단되는지 확인한다.

## 배포 확인

- Render Build가 성공하고 모든 정적 파일이 HTTPS로 200 응답하는지 확인
- 브라우저 콘솔에 JavaScript 오류, CSP 차단, 네트워크 오류가 없는지 확인
- 회원가입 이메일 링크가 Render URL로 돌아오는지 확인
- 360px 화면에서 가로 스크롤과 잘린 버튼이 없는지 확인
- 정상 상태는 Blue, 성공은 Blue/Green, 오류·위험만 Red인지 확인

## 2026-08-12 운영 자동 검증 결과

- 실제 이메일 회원가입, 이메일 확인, 로그아웃, 비밀번호 재로그인 성공
- 미션 120분 완료, 300P 적립, 250P 상품 구매, 최종 잔액 50P 저장 확인
- 포인트 부족 구매와 완료 미션의 추가 진행 차단 확인
- 서로 다른 세션 ID를 가진 독립 인증 컨텍스트 2개에서 동일한 사용자 데이터 조회 확인
- 다른 사용자 인증 컨텍스트에서 개인 프로필·미션·포인트·주문·에어컨 데이터 0건 확인
- 모바일 viewport, 320px 최소 폭, 100% 앱 셸, 4분할 하단 메뉴, safe-area, 600px 확장 레이아웃 정적 검사 통과
- 전체 사용자 여정의 필수 UI hook과 Supabase 원격 저장 함수 회귀 검사 통과
- Render HTML·CSS·JavaScript·런타임 설정 자산 모두 HTTPS 200 응답 확인

다른 기기와 모바일 화면 항목은 실제 물리 기기 대신 독립 인증 컨텍스트와 반응형 구조 자동 검사로 대체했습니다. 실제 브라우저가 연결되는 환경에서는 콘솔 오류와 픽셀 단위 잘림을 추가로 육안 확인할 수 있습니다.
